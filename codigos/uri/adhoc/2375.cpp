#include <bits/stdc++.h>

using namespace std;

int main(int argc, char const *argv[]) {
  int d;
  int a,b,c;
  cin>> d >> a>> b>>c;
  if( a >= d && b >= d && c >= d )
    printf("S\n");
    else
    printf("N\n");

  return 0;
}
